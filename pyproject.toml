[project]
name = "fly-yeet"
version = "0.1.0"
description = "Fly-Yeet: a drone routing simulator — 42 school project"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pydantic>=2.13.4",
    "pygame-ce>=2.5.7",
]
